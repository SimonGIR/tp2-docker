- name: Run HTTP proxy container
  community.docker.docker_container:
    name: httpd
    image: "{{ DOCKERHUB_USERNAME }}/tp-devops-http-server:latest"
    pull: true
    state: started
    restart_policy: always
    recreate: true
    networks:
      - name: app-network
    published_ports:
      - "80:80"