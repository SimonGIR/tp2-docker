all:
  vars:
    ansible_user: admin
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
    #ansible_python_interpreter: /usr/bin/python3.11
    DOCKERHUB_USERNAME: simgir
  children:
    prod:
      hosts:
        simon.girard.20240687.takima.school
